{{- if and .Values.filer.s3.enabled .Values.filer.s3.configSecret.create }}
{{- $secretName := include "seaweedfs.s3SecretName" . }}
{{- $configKey := .Values.filer.s3.configSecret.key | default "s3.json" }}
{{- $existingSecret := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $accessKey := .Values.filer.s3.configSecret.accessKey }}
{{- $secretKey := .Values.filer.s3.configSecret.secretKey }}
{{- if not $accessKey }}
{{- if $existingSecret }}
{{- $accessKey = index $existingSecret.data "accessKey" | b64dec }}
{{- else }}
{{- $accessKey = randAlphaNum 20 }}
{{- end }}
{{- end }}
{{- if not $secretKey }}
{{- if $existingSecret }}
{{- $secretKey = index $existingSecret.data "secretKey" | b64dec }}
{{- else }}
{{- $secretKey = randAlphaNum 40 }}
{{- end }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "seaweedfs.labels" . | nindent 4 }}
    app.kubernetes.io/component: s3
  annotations:
    seaweed.seaweedfs.com/auto-generated: "true"
type: Opaque
stringData:
  accessKey: {{ $accessKey | quote }}
  secretKey: {{ $secretKey | quote }}
  {{ $configKey }}: |
    {
      "identities": [
        {
          "name": "admin",
          "credentials": [
            {
              "accessKey": {{ $accessKey | quote }},
              "secretKey": {{ $secretKey | quote }}
            }
          ],
          "actions": [
            "Admin",
            "Read",
            "List",
            "Tagging",
            "Write"
          ]
        }
      ]
    }
{{- end }}
